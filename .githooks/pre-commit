#!/usr/bin/env bash
# Pre-commit: update marketplace.json version.
# CHANGELOG is handled by commit-msg hook (needs the message).

export HOOK_PENDING=1
VERSION=$(./scripts/set_version.sh)
echo "Version: $VERSION"
